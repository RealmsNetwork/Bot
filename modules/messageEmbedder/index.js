const { SlashCommandBuilder, EmbedBuilder, PermissionFlagsBits } = require('discord.js');
const data=new SlashCommandBuilder().setName('embed').setDescription('Send a customizable embed').addStringOption(o=>o.setName('title').setDescription('Title')).addStringOption(o=>o.setName('description').setDescription('Description').setRequired(true)).addStringOption(o=>o.setName('color').setDescription('Hex color')).addStringOption(o=>o.setName('image').setDescription('Image URL')).addStringOption(o=>o.setName('thumbnail').setDescription('Thumbnail URL')).addChannelOption(o=>o.setName('channel').setDescription('Destination')); 
async function execute(i,client){const c=client.config.messageEmbedder||{};const can=i.memberPermissions.has(PermissionFlagsBits.ManageMessages)||(c.managerRoles||[]).some(r=>i.member.roles.cache.has(String(r)));if(!can)return i.reply({content:'You cannot create embeds.',ephemeral:true});if(Array.isArray(c.allowedChannels)&&c.allowedChannels.length&&!c.allowedChannels.includes(i.channelId))return i.reply({content:'Embeds are not allowed here.',ephemeral:true});const e=new EmbedBuilder().setDescription(i.options.getString('description',true).slice(0,Number(c.maxDescriptionLength||4000)));if(i.options.getString('title'))e.setTitle(i.options.getString('title').slice(0,256));if(i.options.getString('color'))e.setColor(i.options.getString('color'));if(c.allowImages!==false&&i.options.getString('image'))e.setImage(i.options.getString('image'));if(c.allowThumbnails!==false&&i.options.getString('thumbnail'))e.setThumbnail(i.options.getString('thumbnail'));const ch=i.options.getChannel('channel')||i.channel;await ch.send({embeds:[e]});await i.reply({content:`✅ Embed sent to ${ch}.`,ephemeral:true});}
module.exports={commands:[{data,permission:PermissionFlagsBits.ManageMessages,execute}]};
