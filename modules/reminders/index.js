const { SlashCommandBuilder } = require('discord.js');
const command={data:new SlashCommandBuilder().setName('reminder').setDescription('Set a reminder').addIntegerOption(o=>o.setName('minutes').setDescription('Minutes').setMinValue(1).setMaxValue(10080).setRequired(true)).addStringOption(o=>o.setName('text').setDescription('Reminder').setRequired(true)),async execute(i){const minutes=i.options.getInteger('minutes',true),text=i.options.getString('text',true);await i.reply(`⏰ Reminder set for ${minutes}m.`);setTimeout(()=>i.user.send(`⏰ Reminder: ${text}`).catch(()=>{}),minutes*60000);}};module.exports={commands:[command]};
