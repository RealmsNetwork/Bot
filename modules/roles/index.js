const { SlashCommandBuilder, PermissionFlagsBits } = require('discord.js');
const data=new SlashCommandBuilder().setName('role').setDescription('Manage member roles').addSubcommand(s=>s.setName('add').setDescription('Add role').addUserOption(o=>o.setName('user').setDescription('User').setRequired(true)).addRoleOption(o=>o.setName('role').setDescription('Role').setRequired(true))).addSubcommand(s=>s.setName('remove').setDescription('Remove role').addUserOption(o=>o.setName('user').setDescription('User').setRequired(true)).addRoleOption(o=>o.setName('role').setDescription('Role').setRequired(true)));
const command={data,permission:PermissionFlagsBits.ManageRoles,async execute(i){const u=i.options.getUser('user',true),r=i.options.getRole('role',true),m=await i.guild.members.fetch(u.id);if(r.position>=i.guild.members.me.roles.highest.position)return i.reply({content:'That role is not below my highest role.',ephemeral:true});if(i.options.getSubcommand()==='add')await m.roles.add(r);else await m.roles.remove(r);await i.reply(`✅ ${i.options.getSubcommand()==='add'?'Added':'Removed'} ${r} ${i.options.getSubcommand()==='add'?'to':'from'} ${u}.`);}};
module.exports={commands:[command]};
